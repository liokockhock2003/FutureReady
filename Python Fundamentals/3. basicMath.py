# We create a variable 'a' and store the number 25 in it
a = 25

# We create another variable 'b' and store the number 12 in it
b = 12

# This line adds a and b, then prints the result with the label "Addition:"
print("Addition:", a + b)

# This line subtracts b from a, then prints the result with the label "Subtraction:"
print("Subtraction:", a - b)

# This line multiplies a and b, then prints the result with the label "Multiplication:"
print("Multiplication:", a * b)

# This line divides a by b, then prints the result with the label "Division:"
print("Division:", a / b)
